# Copy to .env. These are read by docker-compose for the db service only;
# the Django container gets its own settings from backend/.env.
POSTGRES_DB=Cognitive
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
# Host port for Postgres. 5433 avoids clashing with a local install on 5432.
POSTGRES_PORT=5433
